windows			linux		function

				-v			verbose: displays functions

dir				ls			listing directories and files
				-l 			long listing
				-a 			all

cd				cd			change directory

type						prints content of file

set							displays all environment variables

prompt						modifies prompt shown in command line
$							shows nothing
$p 							path
$g 							greater than sign
$l 							less than sign
$d 							date
$h 							trim one character before it
$t 							time

ping <IP>		ping		sends packets to detect if TCP stack is loaded
-n 							number of packets
-l 							size of packet (can't be more than 65500 in windows)
-4							using IPv4
-6							using IPv6
-t 							overloading
-i 							change TTL

arp -a 						shows arp table
arp -s 						make a static entry in arp table
arp -d 						delete an entry in arp table

tracert			traceroute	trace the route from source to destination

ipconfig		ifconfig	display networking information

				cut

				grep

				sed

				tr